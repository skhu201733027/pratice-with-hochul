/ *
  * 버전 1.0
  * 2022-03-31
  */
#include <stdio.h>

int main()
{
    printf("hello world!");
    printf("충돌 해결");
    int x = 1;
    int y = 2;
    printf("%d + %d = %d\n", x, y, (x + y));

// 1
    printf("%d - %d = %d\n", x, y, (x - y));
    return 0;
}