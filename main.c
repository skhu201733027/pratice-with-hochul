#include <stdio.h>

int main()
{
    printf("hello world!");
    printf("충돌");
    int x = 1;
    int y = 2;
    printf("%d + %d = %d\n", x, y, (x + y));

    printf("%d - %d = %d\n", x, y, (x - y));
    return 0;
}